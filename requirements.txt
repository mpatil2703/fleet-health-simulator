# Required by app.py (the deployed dashboard)
streamlit==1.59.1
pandas==3.0.3
numpy==2.5.1
plotly==6.9.0

# Required only by the analysis/regeneration scripts (not app.py itself),
# kept pinned here so `pip install -r requirements.txt` fully reproduces
# the pipeline described in README.md
scipy==1.18.1
statsmodels==0.14.6
scikit-learn==1.9.0
